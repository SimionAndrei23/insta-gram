import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const redux = createSlice({
    name: sliceName,
    initialState,
    reducers: {
    
    }
});

export const {

} = redux.actions
export default redux.reducer